---
keras:
  backend: tensorflow
logging:
  format: '%(asctime)s: %(name)s: %(levelname)s: %(message)s'
  level: INFO
  path: stdout
  type: stream
multiprocessing:
  enabled: False
  workers: 2
telemetry:
  metrics: True
  tracing: True
